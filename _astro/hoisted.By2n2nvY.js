const s=document.querySelectorAll("[data-reveal]"),o=window.matchMedia("(prefers-reduced-motion: reduce)").matches;if(o||!("IntersectionObserver"in window))s.forEach(e=>e.classList.add("is-visible"));else{const e=new IntersectionObserver(t=>{t.forEach(r=>{r.isIntersecting&&(r.target.classList.add("is-visible"),e.unobserve(r.target))})},{rootMargin:"0px 0px -12% 0px",threshold:.08});s.forEach((t,r)=>{const i=Number(t.getAttribute("data-reveal"))||r%6;t.style.setProperty("--reveal-delay",`${Math.min(i,8)*70}ms`),e.observe(t)})}!o&&window.matchMedia("(hover: hover)").matches&&document.querySelectorAll(".card").forEach(e=>{e.addEventListener("pointermove",t=>{const r=e.getBoundingClientRect();e.style.setProperty("--mx",`${t.clientX-r.left}px`),e.style.setProperty("--my",`${t.clientY-r.top}px`)})});
